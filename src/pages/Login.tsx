import LoginCard from "@/components/LoginCard";

function Login() {
  return (
    <section className="flex justify-center items-center w-full h-dvh bg-light-muted">
      <LoginCard />
    </section>
  );
}

export default Login;
